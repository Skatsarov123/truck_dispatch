{% extends 'base.html.twig' %}

{% block title %}Proposal{% endblock %}

{% block body %}
    <h1>Proposal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proposal.id }}</td>
            </tr>
            <tr>
                <th>ProposalPrice</th>
                <td>{{ proposal.proposalPrice }}</td>
            </tr>
            <tr>
                <th>ProposalLoadingDate</th>
                <td>{{ proposal.proposalLoadingDate ? proposal.proposalLoadingDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ProposalUnloadingDate</th>
                <td>{{ proposal.proposalUnloadingDate ? proposal.proposalUnloadingDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proposal_index') }}">back to list</a>

    <a href="{{ path('proposal_edit', {'id': proposal.id}) }}">edit</a>

    {{ include('proposal/_delete_form.html.twig') }}
{% endblock %}
