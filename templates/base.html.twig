<!DOCTYPE html>
<html lang="en-US">
<head>

    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>{% block title %}TRUCK DISPATCH{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        <link rel="stylesheet" href="{{ asset('css/bootstrap-datetimepicker.min.css') }}">
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
</head>

<body id="{% block body_id %}{% endblock %}">

{% block header %}
    <header>
        <div class="navbar navbar-default navbar-static-top" role="navigation">
            <div class="container">
                <div class="navbar-header">
                    <a href="{{ path('booking_dash') }}" class="navbar-brand">TRUCK DISPATCH</a>
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav navbar-right">
                        {% if app.user %}
                        <li>
                            <a href="{{ path('market_index') }}" class="navbar-brand">
                                Борса
                            </a>
                            <a href="{{ path('customer_index') }}" class="navbar-brand">
                                Клиенти
                            </a>
                            <a href="{{ path('user_profile') }}" class="navbar-brand">
                                Моя Профил
                            </a>
                            <a href="{{ path('driver_index') }}" class="navbar-brand">
                                Шофьори
                            </a>


                            <a href="{{ path('app_logout') }}" class="navbar-brand">
                                Изход
                            </a>
                            {% endif %}
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </header>
{% endblock %}

<div class="container body-container">
    {% block body %}
        <div class="row">
            <div id="main" class="col-sm-9">
                {% block main %}{% endblock %}
            </div>
        </div>
    {% endblock %}
</div>

{% block footer %}
    <footer>
        <div class="container modal-footer">
            <p>&copy; 2020-2021 <a href="https://softbg.net">SoftBG.Net</a></p>
        </div>
    </footer>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/jquery-3.5.1.min.js') }}"></script>
    <script src="{{ asset('js/moment.min.js') }}"></script>
    <script src="{{ asset('js/bootstrap.js') }}"></script>
    <script src="{{ asset('js/bootstrap-datetimepicker.min.js') }}"></script>
    <html>
    <head>
        <meta charset="utf-8">
    </head>

    <style type="text/css" media="all">
        {% block style %}
        #container {
            margin: 0 auto;
            width: 680px
        }

        #seller {
            margin: 25px;
            width: 620px;
            clear: both;
        }

        #invoice, #invoice-total {
            float: right;
            width: 300px;
            margin-bottom: 30px
        }

        #invoice dt, #invoice-total dt {
            float: left;
            width: 100px;
            margin-bottom: 4px
        }

        #invoice dd, #invoice-total dd {
            float: right;
            width: 184px;
            margin: 0 0 4px;
            text-align: right
        }

        .invoice-total {
            float: left;
            font-weight: bold;
            margin: 0;
            padding: 5px 8px;
            border-top: 1px solid #f1f1f1;
            background: #e3e3e3
        }

        #buyer {
            float: left;
            width: 300px
        }

        #invoice-total {
            clear: both;
        }

        #terms, #notes {
            float: left;
            width: 100%;
            margin: 10px 0
        }

        table {
            width: 100%
        }

        table thead tr {
            padding: 10px 5px;
            border: 1px solid #c7c7c7;
            font-weight: bold;
        }

        table thead th {
            border-top: 1px solid #f1f1f1;
            background: #e3e3e3
        }

        table tbody tr {
            padding: 10px 5px;
            border-bottom: 1px solid #e5e5e5;
        }

        .col1 {
            width: 20%;
            text-align: left
        }

        .col2 {
            width: 50%;
            text-align: left
        }

        .col3 {
        }

        .col4 {
        }

        .col5 {
        }

        .col3, .col4, .col5 {
            width: 10%;
            text-align: right
        }

        {% endblock %}
    </style>

    <body>

    <div id="container">
        {% block content %}
        {% endblock %}
    </div>

    </body>
    </html>
{% endblock %}
