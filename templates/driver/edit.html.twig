{% extends 'base.html.twig' %}

{% block title %}Edit Driver{% endblock %}

{% block body %}
    <h1>Редактирай</h1>

    <div class="btn btn-secondary"{{ include('driver/_form.html.twig', {'button_label': 'Запази'}) }}
    <div class="btn btn-secondary"{{ include('driver/_delete_form.html.twig') }}

    <a href="{{ path('driver_index') }}">Назад</a>
{% endblock %}
